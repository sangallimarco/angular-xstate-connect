import { HighlightOnOverDirective } from './highlight-on-over.directive';

describe('HighlightOnOverDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new HighlightOnOverDirective();
  //   expect(directive).toBeTruthy();
  // });
});
